import 'package:flutter/material.dart';

class FieldTrips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Field Trips'),
        centerTitle: true,
      ),
      body: Container(
        child: Text('No Data'),
      ),
    );
  }
}
